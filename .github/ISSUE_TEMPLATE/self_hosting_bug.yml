name: 🐳 Self-hosting / deployment bug
description: Docker setup, migrations, reverse proxy, or other issues running Loomkeep yourself.
labels: ["bug", "docker"]
assignees: Logan2234
body:
  - type: markdown
    attributes:
      value: |
        For bugs *inside* the app itself (a feature not working as expected), use the
        [Bug Reports board](https://feedback.loomkeep.app/board/bug-reports) instead — this
        template is for issues running/deploying your own instance.

  - type: input
    id: version
    attributes:
      label: Loomkeep version
      description: Shown in the Homepage "Version" widget, or `git rev-parse --short HEAD` on the VPS.
      placeholder: "1.3.0 / a1b2c3d"
    validations:
      required: true

  - type: input
    id: setup
    attributes:
      label: Setup
      description: Your `COMPOSE_FILE` line in `.env` (or the `-f` flags you pass), so we know exactly which overrides are active.
      placeholder: "docker/docker-compose.yml:docker/docker-compose.prod.yml:docker/docker-compose.observability.yml"
    validations:
      required: true

  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: What you expected vs. what actually happened.
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Relevant logs
      description: Output of `docker compose logs api` / `docker compose logs web` (redact secrets first).
      render: shell

  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
    validations:
      required: true
