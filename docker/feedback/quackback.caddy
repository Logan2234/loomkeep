# Mounted only when docker-compose.quackback.yml is in use.
# `host.docker.internal` reaches the port Quackback's own, separate compose
# project published on the VPS host — see that compose file.
{$QUACKBACK_SITE_ADDRESS} {
	reverse_proxy host.docker.internal:{$QUACKBACK_APP_PORT}
}
