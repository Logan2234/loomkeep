# Redirects the bare `www.` prefix to the apex. Mounted only by
# docker-compose.prod.yml. Its own site block (not folded into the main
# one) so it can't shadow subdomain blocks like grafana.<domain>.
www.{$SITE_ADDRESS} {
	redir https://{$SITE_ADDRESS}{uri} permanent
}
